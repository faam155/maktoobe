<x-layouts.portal :title="__('ai.new')">
<a class="prompt-back" href="{{ route('ai.index') }}">← {{ __('ai.back') }}</a>
<section class="portal-welcome"><p class="portal-eyebrow">{{ __('ai.title') }}</p><h2>{{ __('ai.new') }}</h2>@if($prompt)<p>{{ __('ai.prompt_context_help') }}</p>@endif</section>
@if($errors->any())<div class="admin-alert is-error" role="alert">{{ $errors->first() }}</div>@endif
@if($models===[])<div class="admin-alert is-error" role="alert">{{ __('ai.no_models') }}</div>@else
<form method="POST" action="{{ route('ai.store') }}" class="ai-compose">@csrf<input type="hidden" name="client_operation_id" value="{{ (string) \Illuminate\Support\Str::uuid() }}">@if($prompt)<input type="hidden" name="prompt_id" value="{{ $prompt->id }}"><div class="ai-selected-prompt"><strong>{{ __('ai.selected_prompt') }}: {{ $prompt->title }}</strong><pre><code>{{ $prompt->content }}</code></pre></div>@endif<label>{{ __('ai.model') }}<select name="model" dir="ltr">@foreach($models as $model)<option value="{{ $model }}" @selected($model===config('ai.default_model'))>{{ $model }}</option>@endforeach</select></label><label>{{ $prompt ? __('ai.additional_context') : __('ai.message') }}<textarea name="content" maxlength="20000" required placeholder="{{ __('ai.message_placeholder') }}">{{ old('content') }}</textarea></label><label class="ai-brand-option"><span><input type="checkbox" name="use_brand_guidelines" value="1" @checked(old('use_brand_guidelines'))> {{ __('brand.use') }}</span><small>{{ __('brand.use_help') }}</small></label><button class="admin-button">{{ __('ai.send') }}</button></form>
@endif
</x-layouts.portal>
