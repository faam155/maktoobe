<x-layouts.portal :title="__('events.events')"><div class="ai-history-hero"><div><h1>{{ __('events.events') }}</h1><p>{{ __('events.portal_intro') }}</p></div></div><form class="ai-history-filters" method="get"><label>{{ __('events.search') }}<input name="search" value="{{ $filters['search']??'' }}"></label><label>{{ __('events.filter') }}<select name="period"><option value="upcoming">{{ __('events.upcoming') }}</option><option value="past" @selected(($filters['period']??'')==='past')>{{ __('events.past') }}</option><option value="all" @selected(($filters['period']??'')==='all')>{{ __('events.all_periods') }}</option></select></label><button>{{ __('events.filter') }}</button></form><div class="event-grid">@forelse($events as $event)<a class="event-card" href="{{ route('events.show',$event) }}"><div><span class="event-badge">{{ __('events.'.$event->status->value) }}</span><h2>{{ $event->title }}</h2><p>{{ $event->category?->name() ?? __('events.no_category') }} · {{ $event->location ?: '—' }}</p></div><time>{{ $event->starts_at->setTimezone(auth()->user()->timezone)->translatedFormat('M j, Y · H:i') }}</time></a>@empty<p class="empty-state">{{ __('events.empty') }}</p>@endforelse</div>{{ $events->links() }}</x-layouts.portal>
