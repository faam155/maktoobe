<x-layouts.admin :title="$prompt->title"><p class="admin-lead">{{ __('prompts.private_preview') }}</p><div class="prompt-detail-meta"><span>{{ __('prompts.statuses.'.$prompt->status->value) }}</span><span>{{ __('prompts.visibilities.'.$prompt->visibility->value) }}</span><span>{{ __('prompts.revision',['number'=>$prompt->revision_number]) }}</span><span>{{ __('prompts.uses') }}: {{ $prompt->uses_count }}</span></div>@if($prompt->description)<p class="prompt-detail-description">{{ $prompt->description }}</p>@endif<div class="prompt-tags">@foreach($prompt->tags as $tag)<span>{{ $tag->display_name }}</span>@endforeach</div><pre class="prompt-content"><code>{{ $prompt->content }}</code></pre><div class="prompt-admin-actions"><a class="admin-button" href="{{ route('admin.prompts.edit',$prompt) }}">{{ __('prompts.edit') }}</a><a href="{{ route('admin.prompts.index') }}">{{ __('prompts.back') }}</a></div></x-layouts.admin>
