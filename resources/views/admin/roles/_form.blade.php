<label>{{ __('admin.role_name') }}<input name="name" value="{{ old('name',$role->name ?? '') }}" required minlength="3" maxlength="64"></label>
@if($permissions->isNotEmpty())<fieldset><legend>{{ __('admin.permission_assignment') }}</legend><p class="admin-muted">{{ __('admin.permission_assignment_hint') }}</p><div class="admin-check-grid">@foreach($permissions as $permission)<label><input type="checkbox" name="permissions[]" value="{{ $permission->name }}" @checked(in_array($permission->name,old('permissions',isset($role)?$role->permissions->pluck('name')->all():[]),true))><span>{{ __('admin.permissions.'.$permission->name) }}</span></label>@endforeach</div></fieldset>@elseif(isset($role))<p class="admin-muted">{{ __('admin.permission_read_only') }}</p>@endif
