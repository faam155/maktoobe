<a class="brand" href="{{ route('foundation') }}" aria-label="{{ __('foundation.brand') }}">
    <span class="brand-mark" dir="ltr" aria-hidden="true">m<span>.</span></span>
    <span class="brand-name">{{ __('foundation.brand') }}<small>{{ __('foundation.brand_arabic') }}</small></span>
</a>
